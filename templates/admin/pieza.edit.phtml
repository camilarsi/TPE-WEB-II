<?php require 'templates/header.phtml' ?>

<h2 class="my-4">Editar Pieza</h2>

<?php if ($error): ?>
    <div class="alert alert-danger"><?= $error ?></div>
<?php endif ?>

<form action="admin/piezas/<?= $pieza->id ?>/editar" method="POST">
    <div class="form-group mb-2">
        <label>Título *</label>
        <input required name="title" type="text" class="form-control" value="<?= $pieza->titulo ?>">
    </div>
    <div class="form-group mb-2">
        <label>Descripción</label>
        <textarea name="description" class="form-control" rows="3"><?= $pieza->descripcion ?></textarea>
    </div>
    <div class="form-group mb-2">
        <label>Materiales *</label>
        <input required name="materiales" type="text" class="form-control" value="<?= $pieza->materiales ?>">
    </div>
    <div class="form-group mb-2">
        <label>Año</label>
        <input name="anio" type="number" min="1900" max="2099" class="form-control" value="<?= $pieza->anio ?>">
    </div>
    <div class="form-group mb-2">
        <label>Serie *</label>
        <select required name="id_serie" class="form-control">
            <?php foreach ($series as $serie): ?>
                <option value="<?= $serie->id ?>" <?= $serie->id == $pieza->id_serie ? 'selected' : '' ?>>
                    <?= $serie->nombre ?>
                </option>
            <?php endforeach ?>
        </select>
    </div>
    <button type="submit" class="btn btn-primary mt-2">Guardar cambios</button>
    <a href="admin/piezas" class="btn btn-secondary mt-2">Cancelar</a>
</form>

<?php require 'templates/footer.phtml' ?>